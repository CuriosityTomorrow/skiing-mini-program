# 滑雪小程序 - 快速启动指南

恭喜！项目基础代码已经实现完成。现在按照以下步骤配置并运行项目。

## 📋 已实现的功能

✅ **DDD架构完整实现**
- 领域层：Resort实体、值对象、仓储接口、领域服务
- 基础设施层：云函数、云函数客户端、仓储实现
- 应用层：DTO、Mapper、应用服务
- 表现层：Hook、页面组件

✅ **云函数**
- resort-search：搜索滑雪场
- resort-detail：获取详情

✅ **前端页面**
- 搜索列表页（支持搜索、筛选、分页）

---

## 🚀 快速启动（5分钟）

### 步骤1：安装依赖

```bash
cd frontend
npm install
```

### 步骤2：配置微信小程序

1. **获取AppID**
   - 访问 [微信公众平台](https://mp.weixin.qq.com/)
   - 注册小程序账号（如果没有）
   - 获取AppID

2. **修改配置文件**
   编辑 `frontend/src/manifest.json`：
   ```json
   {
     "mp-weixin": {
       "appid": "你的AppID",  // 替换这里
       ...
     }
   }
   ```

### 步骤3：开通云开发

1. 在微信小程序后台，进入"云开发"
2. 点击"开通"（免费）
3. 创建环境，记录环境ID

### 步骤4：配置云开发环境ID

编辑 `frontend/src/App.vue`：
```typescript
wx.cloud.init({
  env: '你的环境ID',  // 替换这里
  traceUser: true,
});
```

### 步骤5：创建数据库集合

在微信开发者工具中：
1. 点击"云开发" → "数据库"
2. 点击"添加集合"
3. 输入集合名称：`resorts`
4. 点击"确定"

### 步骤6：导入初始数据

1. 下载初始数据文件
2. 在数据库控制台，点击 `resorts` 集合
3. 点击"导入" → 选择文件类型 "JSON"
4. 导入 `backend/database/init-data/popular-resorts.js` 的数据

或者手动添加一条测试数据：
```json
{
  "name": "将军山滑雪场",
  "type": "outdoor",
  "province": "新疆维吾尔自治区",
  "city": "阿勒泰地区",
  "address": "阿勒泰市将军山",
  "latitude": 47.844,
  "longitude": 88.139,
  "facilities": {
    "hasRental": true,
    "hasParking": true,
    "hasRestaurant": true,
    "hasHotel": true,
    "hasNightSkiing": true
  },
  "openHour": 10,
  "closeHour": 18,
  "tickets": [
    { "type": "成人全天票", "price": 268 }
  ],
  "trails": {
    "totalCount": 40,
    "beginner": 12,
    "intermediate": 18,
    "advanced": 8,
    "expert": 2
  },
  "popularity": 98,
  "rating": 4.8
}
```

### 步骤7：上传云函数

1. 在微信开发者工具中，点击"云开发" → "云函数"
2. 右键 `resort-search` → "上传并部署：云端安装依赖"
3. 右键 `resort-detail` → "上传并部署：云端安装依赖"

等待上传完成（可能需要1-2分钟）

### 步骤8：启动项目

```bash
# 在frontend目录下
npm run dev:mp-weixin
```

### 步骤9：在微信开发者工具中预览

1. 打开微信开发者工具
2. 导入项目：选择 `frontend/dist/dev/mp-weixin` 目录
3. AppID：填入你的AppID
4. 点击"编译"
5. 查看效果

---

## 🎯 测试功能

### 1. 查看热门滑雪场
打开小程序，应该看到默认显示热门滑雪场列表。

### 2. 搜索功能
在搜索框输入：
- "北京" - 搜索北京的滑雪场
- "将军山" - 搜索特定滑雪场
- "新疆" - 搜索省份

### 3. 筛选功能
点击筛选按钮，切换：
- 全部
- 室内
- 室外

### 4. 查看控制台
在微信开发者工具控制台，可以看到：
```
[搜索滑雪场] 参数: {keyword: "", type: undefined, limit: 20, offset: 0}
[搜索滑雪场] 结果数量: 7
[搜索结果] 数量: 7
```

---

## 🔧 开发调试

### 本地调试云函数

1. 在微信开发者工具中
2. 点击"云开发" → "云函数"
3. 右键 `resort-search` → "开启本地调试"
4. 现在云函数会在本地运行，修改代码立即生效

### 查看数据库

1. 点击"云开发" → "数据库"
2. 可以直接查看、添加、修改、删除数据

### 查看云函数日志

1. 点击"云开发" → "云函数"
2. 点击云函数名称
3. 查看"日志"标签

---

## ❓ 常见问题

### Q1: 提示"云函数未找到"

**A**: 检查：
- 云函数是否已上传部署
- 云函数名称是否正确
- 云开发是否已开通

### Q2: 提示"数据库权限错误"

**A**:
- 在云开发控制台，设置 → 数据库权限
- 选择"所有用户可读，仅创建者可读写"

### Q3: 搜索没有结果

**A**:
- 检查数据库是否有数据
- 查看控制台错误信息
- 确认云函数调用成功

### Q4: 页面空白

**A**:
- 检查 pages.json 是否注册了页面
- 查看控制台是否有错误
- 确认编译无错误

### Q5: TypeScript编译错误

**A**:
- 确保已安装依赖 `npm install`
- 重启开发服务器
- 清除缓存重新编译

---

## 📁 项目结构

```
skiing/
├── frontend/                   # 前端项目
│   ├── src/
│   │   ├── domain/            # 领域层
│   │   │   └── resort/
│   │   │       ├── entities/      # 实体
│   │   │       ├── repositories/  # 仓储接口
│   │   │       └── services/      # 领域服务
│   │   ├── application/       # 应用层
│   │   │   ├── dto/               # DTO
│   │   │   ├── mappers/          # Mapper
│   │   │   └── services/         # 应用服务
│   │   ├── infrastructure/   # 基础设施层
│   │   │   └── api/              # API客户端
│   │   ├── interfaces/       # 表现层
│   │   │   ├── hooks/            # 组合式函数
│   │   │   └── pages/            # 页面组件
│   │   ├── shared/           # 共享模块
│   │   └── di/               # 依赖注入
│   ├── dist/                 # 编译输出
│   └── package.json
└── backend/                    # 后端云函数
    └── cloudfunctions/
        ├── resort-search/
        └── resort-detail/
```

---

## 🎨 下一步

现在基础框架已经搭建完成，你可以：

1. ✅ **测试搜索功能**
   - 尝试不同的搜索关键词
   - 测试筛选功能

2. 📝 **添加更多滑雪场数据**
   - 通过数据库控制台手动添加
   - 或编写数据导入脚本

3. 🎨 **自定义UI样式**
   - 修改 `list/index.vue` 的样式
   - 调整颜色、布局等

4. 🚀 **开发详情页**
   - 实现滑雪场详情页面
   - 展示更多信息

5. ⭐ **收藏功能**
   - 实现用户登录
   - 添加收藏功能

---

## 💡 开发提示

### 修改代码后的调试流程

1. **修改前端代码**
   ```bash
   # 代码会自动重新编译
   # 在微信开发者工具中点击"编译"查看效果
   ```

2. **修改云函数代码**
   - 本地调试模式：自动生效
   - 云端模式：需要重新上传部署

3. **修改数据库**
   - 直接在数据库控制台操作
   - 立即生效

### 常用命令

```bash
# 开发
npm run dev:mp-weixin

# 构建
npm run build:mp-weixin

# 类型检查
npx vue-tsc --noEmit
```

---

## 📞 需要帮助？

如果遇到问题：

1. 查看控制台错误信息
2. 检查云函数日志
3. 确认配置是否正确
4. 告诉我具体问题，我会帮你解决

---

**准备好了吗？开始运行你的第一个小程序吧！** 🎉
