# 微信开发者工具调试指南

## 错误 6000100 解决方案

### ✅ 已修复
- 移除了 `manifest.json` 中的 `cloudfunctionRoot` 配置
- 移除了 `App.vue` 中的云开发初始化代码

### 现在请重新编译
1. 在 HBuilderX 中停止运行
2. 重新运行到微信开发者工具
3. 错误应该消失了

---

## 微信开发者工具配置（不同版本界面可能不同）

### 方法1：工具栏配置
点击微信开发者工具顶部菜单：
- **工具** → **构建 npm**（如果需要）
- **工具** → **清除缓存** → **清除全部缓存**

### 方法2：项目配置
点击右上角 **详情** 或 **设置** 按钮，查看：

#### 「本地设置」标签
- ✅ 不校验合法域名、web-view、TLS版本以及HTTPS证书
- ✅ 启用 ES6 转 ES5
- ✅ 启用增强编译
- ✅ 使用 npm 模块

#### 「调试基础库」
选择一个较新的版本（如 2.30.0 以上）

---

## 关于自动刷新

### 正常的保存流程
1. 在 HBuilderX 中修改代码
2. 按 `Cmd + S` (Mac) 或 `Ctrl + S` (Windows) 保存
3. 微信开发者工具**应该自动刷新**（2-5秒）

### 如果没有自动刷新
**方法1：手动触发**
- 在微信开发者工具中点击 **编译** 按钮
- 或按快捷键（通常是 Ctrl+B）

**方法2：检查文件监听**
- 确保 HBuilderX 和微信开发者工具都正常运行
- 关闭后重新启动

**方法3：使用微信开发者工具直接打开**
- 不通过 HBuilderX
- 直接在微信开发者工具中打开项目文件夹
- 这样自动刷新更稳定

---

## 控制台查看

### 打开控制台
在微信开发者工具底部：
- 点击 **控制台** (Console) 标签
- 或按 `F12` 打开调试工具

### 应该看到的日志
```
[App] 应用启动
[App] 当前模式：Mock 数据模式
[测试] 页面已加载
[测试] 搜索按钮被点击
[测试] 数据已设置
```

### 常见错误类型

#### 错误：module not found
- 说明：导入路径错误
- 解决：检查 import 语句的路径是否正确

#### 错误：diContainer is not defined
- 说明：忘记导入依赖注入容器
- 解决：检查是否添加了 `import { diContainer } from ...`

#### 错误：wx.cloud is not defined
- 说明：云开发未初始化（Mock模式下正常）
- 解决：确保使用 Mock 模式

---

## 如何给我截图

### 方法1：直接拖拽
1. 截图保存到桌面
2. 直接拖拽图片文件到对话框

### 方法2：粘贴
1. 截图后复制到剪贴板
2. 在对话框中按 `Cmd + V` (Mac) 或 `Ctrl + V` (Windows)

### 方法3：使用微信开发者工具的截图
1. 在微信开发者工具中点击 **调试器**
2. 使用截图工具（如果有的话）
3. 粘贴到这里

---

## 测试步骤

### 1️⃣ 测试基础页面
保存后应该看到：
- 蓝色按钮
- "滑雪场列表" 标题
- "滑雪场数量：2"

### 2️⃣ 测试控制台
控制台应该显示：
```
[测试] 页面已加载
[测试] 搜索按钮被点击
[测试] 数据已设置
```

### 3️⃣ 如果还是白屏
请截图给我：
- 整个微信开发者工具窗口
- 控制台的错误信息
- 调试器 Network 标签（看看有没有请求失败）

---

## 下一步

如果测试页面正常显示，我会恢复完整的滑雪场列表功能。
